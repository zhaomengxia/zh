<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zh.mapper.sys.SysRolesMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zh.entity.sys.SysRoles">
        <id column="id" property="id" />
        <result column="role_name" property="roleName" />
        <result column="description" property="description" />
        <result column="is_deleted" property="deleted" />
        <result column="parent_id" property="parentId" />
        <result column="role_type" property="roleType" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>


    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultDtoMap" type="com.zh.dto.sys.role.SysRoleDTO">
        <id column="id" property="id" />
        <result column="role_name" property="roleName" />
        <result column="description" property="description" />
        <result column="parent_id" property="parentId" />
        <result column="role_type" property="roleType" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, role_name, description,parent_id,role_type, is_deleted, create_time, update_time
    </sql>

    <!-- 通用查询结果列 -->
    <sql id="Dto_Column_List">
        id, role_name, description,parent_id,role_type, is_deleted, create_time, update_time
    </sql>

    <select id="selectAll"  resultMap="BaseResultDtoMap">
        SELECT <include refid="Dto_Column_List"/>
        FROM sys_roles
        WHERE IS_DELETED = 0
    </select>
    <select id="checkNameIsSame" resultMap="BaseResultDtoMap">

        SELECT <include refid="Dto_Column_List"/>
        FROM sys_roles
        WHERE IS_DELETED = 0 and role_name=#{name}

    </select>
    <select id="selectByIdAsParentId" resultMap="BaseResultDtoMap">

        SELECT <include refid="Dto_Column_List"/>
        FROM sys_roles
        WHERE IS_DELETED = 0 and parent_id=#{id}

    </select>

</mapper>
